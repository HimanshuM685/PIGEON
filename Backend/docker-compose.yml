services:
  pigeon-backend:
    build: .
    container_name: pigeon-backend
    restart: unless-stopped
    ports:
      - "${PORT:-6969}:6969"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
