services:
  pigeon-backend:
    build: .
    container_name: pigeon-backend
    restart: unless-stopped
    ports:
      - "${PORT:-7575}:7575"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
