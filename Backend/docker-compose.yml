services:
  pigeon-backend:
    build: .
    container_name: pigeon-backend
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
